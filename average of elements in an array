#include <iostream>
using namespace std;
int main (){
int arr[]={1,2,3,4};
  int size=sizeof(arr)/sizeof(arr[0]);
int sum=0;
  float avg;
  for(int i=0;i<size;i++){
    sum+=arr[i];
  }
  avg=(float)sum/size;
  cout<<"avg:"<<avg<<endl;
}
