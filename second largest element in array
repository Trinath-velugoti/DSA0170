#include <iostream>
using namespace std;
int main (){
  int l1=0,l2=0,i;
  int arr[]={1,2,4,5};
  int n=sizeof(arr)/sizeof(arr[0]);
 for(i=0;i<n;i++){
   if(arr[i]>l1){
     l2=l1;
     l1=arr[i];
   }
   if(arr[i]>l2&&arr[i]<l2){
     l2=arr[i];
 }
 }
  cout<<l2;
}
