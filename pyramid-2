#include <iostream>
using namespace std;
int main (){
  int i,j,k,n=5,num=0;
  for(i=1;i<=n;i++){
    for(j=n;i>i;j--){
      cout<<" ";
    }
    for(j=1;j<=i;j++){
      num++;
      cout<<num<<" ";
    }
    for(k=1;k<i;k++){
      num--;
      cout<<num<< " ";
    }
    cout<<endl;
  }
}
